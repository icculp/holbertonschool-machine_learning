#!/usr/bin/env python3
"""
    Dimensionality Reduction
    Only numpy allowed
    Your code should use the minimum number of
        operations to avoid floating point errors
"""
import numpy as np


def Q_affinities(Y):
    """ doc
    """
    Q = np.ndarray((X.shape[0], X.shape[0]))
    num = np.ndarray((X.shape[0], X.shape[0]))
    return Q, num
