#!/usr/bin/env python3
"""
    Task 4
"""


def add_arrays(arr1, arr2):
    """ Adds two arrays of same size, else returns None """
    try:
        new = arr1.copy()
        for i in range(size(arr1)):
            for j in range(size[
    except Exception:
        row = []
        for j in range(rows):
            row.append(matrix[j][i])
        tranny.append(row.copy())
    return tranny
